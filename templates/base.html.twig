<!DOCTYPE html >
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <meta name="description" content="Upload your heavy files with Bigupload">
        <meta name="keywords" content="Upload files, heavy files, files transfer, Bigupload">
        <meta name="author" content="Rapkalin">
        <title>{% block title %}Bigupload{% endblock %}</title>
        <link rel="icon" href="{{  asset("styles/images/favicon.svg") }}" sizes="any" type="image/svg+xml" >
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
            <script defer type="text/javascript" src={{ asset("library/jquery/jquery.js") }}></script>
            <script type="text/javascript" src={{ asset("library/plupload/plupload.full.min.js") }}></script>
            <script defer src="https://kit.fontawesome.com/a8ed703567.js" crossorigin="anonymous"></script>
            {% block importmap %}
                {{ importmap('app') | raw | replace({'<script': '<script defer nonce="' ~ csp_nonce('script') ~ '"' }) | raw }}
            {% endblock %}
        {% endblock %}
    </head>
    <body>
        {% block body %}{% endblock %}
    </body>
</html>
